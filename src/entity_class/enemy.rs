use bevy::prelude::*;

pub fn spawn_enemy(
    mut c: &mut Commands,
    assets: &Res<AssetServer>,
    mut materials: &mut ResMut<Assets<ColorMaterial>>,
    transform: &Transform,
) {
    // todo!()
}
